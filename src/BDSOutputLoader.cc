#include "BDSOutputLoader.hh"

#include "parser/options.h"

#include "globals.hh" // geant4 types / globals


GMAD::Options BDS::LoadOptions(G4String filepath)
{
  // load options

  return GMAD::Options();
}
