d1: drift, l=0.5;

sh1 : shield, l=3*m,
      	      aper1=3*cm,
	      aper2=1.5*cm,
	      apertureType="rectangular",
	      outerDiameter=240*cm,
	      beampipeThickness=1*mm,
	      xsize=10*cm,
	      ysize=10*cm,
	      outerMaterial="concrete",
	      beampipeMaterial="stainlesssteel";

d2: drift, l=0.5;

lat : line = (d1,sh1,d2);
com : line = (sh1);

include optionsTest.gmad;