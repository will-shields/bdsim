d1: drift, l=0.2, aper1=3*cm, aper2=1.5*cm, apertureType="rectangular";

sh1 : shield, l=0.2*m,
      	      aper1=3*cm,
	      aper2=1.5*cm,
	      apertureType="rectangular",
	      outerDiameter=50*cm,
	      beampipeThickness=1*mm,
	      xsize=7*cm,
	      ysize=7*cm,
	      material="concrete",
	      beampipeMaterial="stainlesssteel",
              colour="iron";

lat : line = (d1,sh1,d1);
com : line = (sh1);

include optionsTest.gmad;