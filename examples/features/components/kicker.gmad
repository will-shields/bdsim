d1: drift, l=0.5;
k1 : kicker, l=0.5*m, hkick=1e-2, vkick=1e-2;
d2: drift, l=0.5;
k2 : kicker, l=0.5*m;

lat : line = (d1,k1,d2,k2);
com : line = (k1);

use, period=lat;

include options.gmad;

! need 0,0 beam to test these properly

beam, particle="e-",
      energy=1.3*GeV,
      distrType="reference";
