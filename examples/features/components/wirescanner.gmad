d1: drift, l=0.5;
w1: wirescanner, l=0.1*m, outerDiameter=10*cm, wireDiameter=0.005*m, wireLength=0.02*m, wirescannerOffset=0*cm, wirescannerRotx=1.57, wirescannerRoty=0;

lat : line = (d1, w1, d1);
com : line = (w1);

include optionsTest.gmad;
