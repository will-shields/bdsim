simple_testing(tunnel-long-straight-elliptical                "--file=1a_elliptical.gmad"      ${OVERLAP_CHECK})
simple_testing(tunnel-long-straight-rectangular               "--file=1b_rectangular.gmad"     ${OVERLAP_CHECK})
simple_testing(tunnel-long-straight-circular                  "--file=1c_circular.gmad"        ${OVERLAP_CHECK})
simple_testing(tunnel-long-straight-square                    "--file=1d_square.gmad"          ${OVERLAP_CHECK})
simple_testing(tunnel-long-straight-rectaboveground           "--file=1e_rectaboveground.gmad" ${OVERLAP_CHECK})
simple_testing(tunnel-long-straight-following-elliptical      "--file=2a_elliptical.gmad"      ${OVERLAP_CHECK})
simple_testing(tunnel-long-straight-following-rectangular     "--file=2b_rectangular.gmad"     ${OVERLAP_CHECK})
simple_testing(tunnel-long-straight-following-circular        "--file=2c_circular.gmad"        ${OVERLAP_CHECK})
simple_testing(tunnel-long-straight-following-square          "--file=2d_square.gmad"          ${OVERLAP_CHECK})
simple_testing(tunnel-long-straight-following-rectaboveground "--file=2e_rectaboveground.gmad" ${OVERLAP_CHECK})
simple_testing(tunnel-initial-bend-elliptical                 "--file=3a_elliptical.gmad"      ${OVERLAP_CHECK})
simple_testing(tunnel-initial-bend-rectangular                "--file=3b_rectangular.gmad"     ${OVERLAP_CHECK})
simple_testing(tunnel-initial-bend-circular                   "--file=3c_circular.gmad"        ${OVERLAP_CHECK})
simple_testing(tunnel-initial-bend-square                     "--file=3d_square.gmad"          ${OVERLAP_CHECK})
simple_testing(tunnel-initial-bend-rectaboveground            "--file=3e_rectaboveground.gmad" ${OVERLAP_CHECK})
simple_testing(tunnel-several-bends-elliptical                "--file=4a_elliptical.gmad"      ${OVERLAP_CHECK})
simple_testing(tunnel-several-bends-rectangular               "--file=4b_rectangular.gmad"     ${OVERLAP_CHECK})
simple_testing(tunnel-several-bends-circular                  "--file=4c_circular.gmad"        ${OVERLAP_CHECK})
simple_testing(tunnel-several-bends-square                    "--file=4d_square.gmad"          ${OVERLAP_CHECK})
simple_testing(tunnel-several-bends-rectaboveground           "--file=4e_rectaboveground.gmad" ${OVERLAP_CHECK})
simple_testing(tunnel-several-bends-two-way-elliptical        "--file=5a_elliptical.gmad"      ${OVERLAP_CHECK})
simple_testing(tunnel-several-bends-two-way-rectangular       "--file=5b_rectangular.gmad"     ${OVERLAP_CHECK})
simple_testing(tunnel-several-bends-two-way-circular          "--file=5c_circular.gmad"        ${OVERLAP_CHECK})
simple_testing(tunnel-several-bends-two-way-square            "--file=5d_square.gmad"          ${OVERLAP_CHECK})
simple_testing(tunnel-several-bends-two-way-rectaboveground   "--file=5e_rectaboveground.gmad" ${OVERLAP_CHECK})
simple_testing(tunnel-very-long-following-elliptical          "--file=6a_elliptical.gmad"      ${OVERLAP_CHECK})
simple_testing(tunnel-very-long-following-rectangular         "--file=6b_rectangular.gmad"     ${OVERLAP_CHECK})
simple_testing(tunnel-very-long-following-circular            "--file=6c_circular.gmad"        ${OVERLAP_CHECK})
simple_testing(tunnel-very-long-following-square              "--file=6d_square.gmad"          ${OVERLAP_CHECK})
simple_testing(tunnel-very-long-following-rectaboveground     "--file=6e_rectaboveground.gmad" ${OVERLAP_CHECK})
simple_testing_long(tunnel-long-arc-elliptical_LONG           "--file=7a_elliptical.gmad"      ${OVERLAP_CHECK})
simple_testing_long(tunnel-long-arc-rectangular_LONG          "--file=7b_rectangular.gmad"     ${OVERLAP_CHECK})
simple_testing_long(tunnel-long-arc-circular_LONG             "--file=7c_circular.gmad"        ${OVERLAP_CHECK})
simple_testing_long(tunnel-long-arc-square_LONG               "--file=7d_square.gmad"          ${OVERLAP_CHECK})
simple_testing_long(tunnel-long-arc-rectaboveground_LONG      "--file=7e_rectaboveground.gmad" ${OVERLAP_CHECK})
simple_testing_long(tunnel-samplers-elliptical_LONG           "--file=8a_elliptical.gmad"      ${OVERLAP_CHECK})
simple_testing_long(tunnel-samplers-rectangular_LONG          "--file=8b_rectangular.gmad"     ${OVERLAP_CHECK})
simple_testing_long(tunnel-samplers-circular_LONG             "--file=8c_circular.gmad"        ${OVERLAP_CHECK})
simple_testing_long(tunnel-samplers-square_LONG               "--file=8d_square.gmad"          ${OVERLAP_CHECK})
simple_testing_long(tunnel-samplers-rectaboveground_LONG      "--file=8e_rectaboveground.gmad" ${OVERLAP_CHECK})
