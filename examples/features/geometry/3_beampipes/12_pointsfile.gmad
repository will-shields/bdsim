d1: drift, l=0.2*m, apertureType="pointsfile:12_points.dat:cm", beampipeThickness=2*mm;
d2: drift, l=0.2*m, apertureType="pointsfile:12_points.dat:cm", beampipeThickness=2*mm, tilt=pi/20;

l1: line = (d1, d2);

use, period = l1;

sample, all;

include options.gmad;
