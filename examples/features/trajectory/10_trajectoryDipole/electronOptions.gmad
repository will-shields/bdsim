! standard options for runing bdsim

rangecut = 1*mm;

option, beampipeRadius = 10 * cm,
        outerDiameter = 1.9 * m,
        samplerDiameter = 2.0*m,
        beampipeThickness = 0.5 * mm,
	prodCutPhotons   = rangecut,
	prodCutElectrons = rangecut,
	prodCutPositrons = rangecut,
	defaultRangeCut  = rangecut;

! option,physicsList="";
option,storeTrajectories=1;
option,storeTrajectoryDepth=0;
option,seed=1;
option,maximumStepLength=0.1*m;
option,nturns=1;