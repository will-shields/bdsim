# high precision tests take a while to load neutron data, so labelled long
simple_testing(physics_charge_exchange        "--file=charge_exchange.gmad"        ${OVERLAP_CHECK})
simple_testing(physics_decay                  "--file=decay.gmad"                  ${OVERLAP_CHECK})
simple_testing(physics_decay_radioactive      "--file=decay_radioactive.gmad"      ${OVERLAP_CHECK})
simple_testing(physics_em                     "--file=em.gmad"                     ${OVERLAP_CHECK})
simple_testing(physics_em_extra               "--file=em_extra.gmad"               ${OVERLAP_CHECK})
simple_testing(physics_em_penelope            "--file=em_penelope.gmad"            ${OVERLAP_CHECK})
simple_testing(physics_em_livermore           "--file=em_livermore.gmad"           ${OVERLAP_CHECK})
simple_testing(physics_em_livermore_polarised "--file=em_livermore_polarised.gmad" ${OVERLAP_CHECK})
simple_testing(physics_em_low_ep              "--file=em_low_ep.gmad"              ${OVERLAP_CHECK})
simple_testing(physics_em_ss                  "--file=em_ss.gmad"                  ${OVERLAP_CHECK})
simple_testing(physics_em_wvi                 "--file=em_wvi.gmad"                 ${OVERLAP_CHECK})
simple_testing(physics_em_1                   "--file=em_1.gmad"                   ${OVERLAP_CHECK})
simple_testing(physics_em_2                   "--file=em_2.gmad"                   ${OVERLAP_CHECK})
simple_testing(physics_em_3                   "--file=em_3.gmad"                   ${OVERLAP_CHECK})
simple_testing(physics_em_4                   "--file=em_3.gmad"                   ${OVERLAP_CHECK})
simple_testing(physics_ftfp_bert              "--file=ftfp_bert.gmad"              ${OVERLAP_CHECK})
simple_testing_long(physics_ftfpberthp        "--file=ftfp_bert_hp.gmad"           ${OVERLAP_CHECK})
simple_testing(physics_hadronic_elastic       "--file=hadronic_elastic.gmad"       ${OVERLAP_CHECK})
simple_testing(physics_hadronic_elastic_d     "--file=hadronic_elastic_d.gmad"     ${OVERLAP_CHECK})
simple_testing(physics_hadronic_elastic_h     "--file=hadronic_elastic_h.gmad"     ${OVERLAP_CHECK})
simple_testing(physics_hadronic_elastic_hp    "--file=hadronic_elastic_hp.gmad"    ${OVERLAP_CHECK})
simple_testing(physics_hadronic_elastic_xs    "--file=hadronic_elastic_xs.gmad"    ${OVERLAP_CHECK})
simple_testing(physics_ion                    "--file=ion.gmad"                    ${OVERLAP_CHECK})
simple_testing(physics_ion_elastic            "--file=ion_elastic.gmad"            ${OVERLAP_CHECK})
simple_testing(physics_ion_elastic_qmd        "--file=ion_elastic_qmd.gmad"        ${OVERLAP_CHECK})
simple_testing(physics_muon                   "--file=muon.gmad"                   ${OVERLAP_CHECK})
simple_testing(physics_optical                "--file=optical.gmad"                ${OVERLAP_CHECK})
simple_testing(physics_qgsp_bert              "--file=qgsp_bert.gmad"              ${OVERLAP_CHECK})
simple_testing_long(physics_qgsp_bert_hp      "--file=qgsp_bert_hp.gmad"           ${OVERLAP_CHECK})
simple_testing(physics_qgsp_bic               "--file=qgsp_bic.gmad"               ${OVERLAP_CHECK})
simple_testing_long(physics_qgsp_bic_hp       "--file=qgsp_bic_hp.gmad"            ${OVERLAP_CHECK})
simple_testing(physics_synch_rad              "--file=synch_rad.gmad"              ${OVERLAP_CHECK})

if (G4_MINOR_VERSION GREATER 1)
  simple_testing(physics_alias_spindecay   "--file=alias_cerenkov.gmad"     ${OVERLAP_CHECK})
  simple_testing(physics_cherenkov         "--file=cherenkov.gmad"          ${OVERLAP_CHECK})
  simple_testing(physics_em_gs             "--file=em_gs.gmad"              ${OVERLAP_CHECK})
  simple_testing(physics_decay_spin        "--file=decay_spin.gmad"         ${OVERLAP_CHECK})
endif()

if (G4_MINOR_VERSION GREATER 3)
  simple_testing(physics_decay_muonic_atom "--file=decay_muonic_atom.gmad"  ${OVERLAP_CHECK})
endif()

# test invalid physics list
simple_fail(physics_invalid          "--file=gibberish.gmad"        ${OVERLAP_CHECK})

# these require the optional G4PARTICLEHPDATA set from the Geant4 website
if($ENV{G4PARTICLEHPDATA})
  simple_testing(physics_hadronic_elastic_lend  "--file=hadronic_elastic_lend.gmad" ${OVERLAP_CHECK})
  if (G4_MINOR_VERSION GREATER 2 OR (G4_MINOR_VERSION EQUAL 2 AND G4_PATCH_LEVEL GREATER 2))
    simple_testing(physics_ion_php                "--file=ion_php.gmad"               ${OVERLAP_CHECK})
    simple_testing(physics_alias_ionphp           "--file=alias_ionphp.gmad"          ${OVERLAP_CHECK})
  endif()
  simple_testing(physics_ion_inclxx             "--file=ion_inclxx.gmad"            ${OVERLAP_CHECK})
  simple_testing(physics_alias_ioninclxx        "--file=alias_ioninclxx.gmad"       ${OVERLAP_CHECK})
  simple_testing(physics_ion_binary             "--file=ion_binary.gmad"            ${OVERLAP_CHECK})
  simple_testing(physics_alias_ionbinary        "--file=alias_ionbinary.gmad"       ${OVERLAP_CHECK})
endif()

#aliases
simple_testing(physics_alias_cerenkov    "--file=alias_cerenkov.gmad"     ${OVERLAP_CHECK})
simple_testing(physics_alias_emlow       "--file=alias_emlow.gmad"        ${OVERLAP_CHECK})
simple_testing(physics_alias_hadronic_hp "--file=alias_hadronic_hp.gmad"  ${OVERLAP_CHECK})


simple_testing(physics_alias_synchrad    "--file=alias_synchrad.gmad"     ${OVERLAP_CHECK})
