t1: target, l=200*m, outerDiameter=20*m, material="copper";
l1: line = (t1);
use, l1;

option, physicsList="gamma_to_mumu";

option, checkOverlaps=1,
	worldMaterial="vacuum";

beam, particle="gamma",
      energy=100*GeV;